//=============================================================================
// Alarm Config Enums
//=============================================================================

// Standard statistics enum (fixed values)

export enum StandardStatistic {
  SAMPLE_COUNT = 'SampleCount',
  AVERAGE = 'Average',
  SUM = 'Sum',
  MINIMUM = 'Minimum',
  MAXIMUM = 'Maximum',
  IQM = 'IQM',
}

